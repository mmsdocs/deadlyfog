draw_set_colour(color);
draw_set_alpha(alpha);
draw_set_halign(halign);
draw_set_valign(valign);

x = current_position_x + padding_left - padding_right;

y = current_position_y + padding_top - padding_bottom;
